**AI-Generated Text Detection using Bidirectional LSTM**

This project implements a custom Bidirectional LSTM (Long Short-Term Memory) model designed to determine whether a given text is AI-generated or human-written.

The model architecture is built from scratch with custom LSTM layers, allowing fine-tuned control over sequence processing and contextual understanding.

A live demo of the project is available here:
